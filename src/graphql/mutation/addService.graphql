mutation AddService($input: ServiceLog_Input!) {
  addService(input: $input) {
    errors {
      field
      message
    }
    servicelog {
      id
      service_id
      patient_id
      filenumber
      assignee_id
      date
      createdAt
      updatedAt
    }
  }
}

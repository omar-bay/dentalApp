mutation CreateFile($input: File_Input!) {
  createFile(input: $input) {
    errors {
      field
      message
    }
    file {
      file_number
      patient_id
      status
      assignee_id
      createdAt
      updatedAt
    }
  }
}

mutation CreatePatient($input: Patient_Input!) {
  createPatient(input: $input) {
    id
    file_number
    name
    gender
    cat_id
    dateOfBirth
    profile_pic_url
    createdAt
    updatedAt
  }
}

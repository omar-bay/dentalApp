mutation CreateTask($input: Task_Input!) {
  createTask(input: $input) {
    errors {
      field
      message
    }
    task {
      id
      service_log_id
      stage
      name
      description
      assignee_notes
      date
      createdAt
      updatedAt
    }
  }
}

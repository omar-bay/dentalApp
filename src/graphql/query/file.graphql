query File($fileNumber: Float!) {
  file(file_number: $fileNumber) {
    errors {
      field
      message
    }
    file {
      file_number
      patient_id
      status
      assignee_id
      createdAt
      updatedAt
    }
  }
}

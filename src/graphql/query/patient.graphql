query Patient($patientId: Float!) {
  patient(id: $patientId) {
    errors {
      field
      message
    }
    patient {
      id
      file_number
      name
      profile_pic_url
      gender
      dateOfBirth
      cat_id
      createdAt
      updatedAt
    }
  }
}

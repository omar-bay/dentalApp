query TasksByService($serviceLogId: Int!) {
  tasksByService(service_log_id: $serviceLogId) {
    id
    service_log_id
    stage
    name
    description
    assignee_notes
    date
    createdAt
    updatedAt
  }
}

import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    root: {

    },
    headerContainer: {
        backgroundColor: 'red',
        height: 50
    },
    headerTitle: {}
})

export default styles
import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    header: {
        position: 'absolute',
        left: 0,
        right: 0,
        top: -1,
        zIndex: 1
    },
    scroll_view: {
        flexGrow: 1,
        width: '100%',
    },
})

export default styles